# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ZAContactPickerUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  #use_frameworks!
  # Pods for ZAContactPickerUI
  pod 'IGListKit', '~> 3.4.0'

  target 'ZAContactPickerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
